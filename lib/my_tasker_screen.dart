import 'package:flutter/material.dart';

class MyTaskerScreen extends StatelessWidget {
  const MyTaskerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("My Tasker"),
      ),
    );
  }
}